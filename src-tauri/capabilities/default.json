{
  "identifier": "default",
  "description": "Default capability for SkillForge",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "shell:default"
  ]
}
